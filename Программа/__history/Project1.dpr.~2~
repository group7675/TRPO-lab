program Project1;

uses
  Vcl.Forms,
  Unit1 in 'Unit1.pas' {Start},
  Unit2 in 'Unit2.pas' {Menu};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TStart, Start);
  Application.CreateForm(TMenu, Menu);
  Application.Run;
end.
